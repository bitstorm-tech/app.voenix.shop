########################################
# Database
########################################
# Default to sqlite file (local dev)
DATABASE_URL=sqlite:///./app.db
# Example Postgres URL (uncomment to use Postgres)
# DATABASE_URL=postgres://{user}:{password}@localhost:5432/voenix?sslmode=disable

########################################
# Migrations
########################################
# Run GORM automigrations for domain tables on startup
AUTO_MIGRATE=true

########################################
# Sessions
########################################
# Session TTL in seconds (default 7 days)
SESSION_TTL_SECONDS=604800

########################################
# Server
########################################
# Bind address for the Go server
ADDR=:8081
# Comma-separated; include * to allow any (dev only)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Optional: Absolute or relative path to the built frontend (Vite dist)
# When set, backend serves the SPA at / with /assets/* and SPA fallback
# Example for local dev after `npm run build` from repo root:
# FRONTEND_DIST=./frontend/dist
FRONTEND_DIST=

########################################
# Storage
########################################
# Filesystem root for images (required by internal/image)
# Example structure will be created as needed:
#   ${STORAGE_ROOT}/public/images
#   ${STORAGE_ROOT}/private/images
# NOTE: Inside the Docker image, the default is /app/storage
STORAGE_ROOT=./storage

########################################
# AI (common config)
########################################
# When true, force the mock AI generator and bypass external API calls
TEST_MODE=false

########################################
# AI (Google)
########################################
# Required to enable /api/ai/images endpoints
GOOGLE_API_KEY=
# Optional; defaults to gemini-2.5-flash-image-preview when empty
GEMINI_IMAGE_MODEL=

########################################
# AI (OpenAI)
########################################
# Required to enable /api/ai/images endpoints
OPENAI_API_KEY=
# Optional; defaults to gpt-image-1 when empty
OPENAI_IMAGE_MODEL=

########################################
# Order PDF SFTP Upload
########################################
# Hostname (optionally host:port). Prefix with sftp:// (default) or ssh://.
ORDER_PDF_FTP_SERVER=
ORDER_PDF_FTP_FOLDER=
# Credentials for SFTP upload.
ORDER_PDF_FTP_USER=
ORDER_PDF_FTP_PASSWORD=
# Optional timeout in seconds (defaults to 10 when unset or zero)
ORDER_PDF_FTP_TIMEOUT=10
# Optional boolean to skip host key verification (defaults to false)
ORDER_PDF_FTP_SKIP_HOST_KEY_VERIFICATION=false
# Optional absolute path to a known_hosts file (defaults to ~/.ssh/known_hosts)
ORDER_PDF_FTP_KNOWN_HOSTS_PATH=
# Optional fallback filename when ORDER_PDF_FTP_FOLDER points to a directory
ORDER_PDF_FTP_FALLBACK_FILENAME=
